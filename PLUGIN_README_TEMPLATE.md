# <插件名称>

<!--
本文件是 astrbot_plugins 仓库下所有插件子模块 README 的编写模板。
新增或维护子模块时，请复制本模板到对应子仓库的 README.md 并按实际情况填写，
不得删减「指令列表」「函数调用 / 对外接口」「附加功能」三个必填章节。
使用时删除本注释块。
-->

一句话描述插件的核心用途。

- **插件名**：`plugin_name`
- **版本**：`1.0.0`
- **作者**：`your_name`
- **适配 AstrBot 版本**：`>= x.y.z`
- **仓库地址**：`https://github.com/<owner>/<repo>`

## 功能特性

- 特性一：简要说明
- 特性二：简要说明

## 安装

```bash
# 在 AstrBot 插件目录下
git clone https://github.com/<owner>/<repo>.git
pip install -r requirements.txt
```

## 配置项

| 配置键 | 类型 | 默认值 | 说明 |
| --- | --- | --- | --- |
| `example_key` | `str` | `""` | 配置项作用说明 |

## 指令列表

> 必填章节。列出该插件注册的所有用户可用指令。

| 指令 | 参数 | 权限 | 说明 | 示例 |
| --- | --- | --- | --- | --- |
| `/example` | `<arg1> [arg2]` | 所有人 / 管理员 | 指令作用说明 | `/example foo bar` |

## 函数调用 / 对外接口

> 必填章节。列出该插件暴露给其他插件或开发者调用的公共函数、类、事件钩子（若插件不对外提供调用能力，需明确写"本插件不提供对外接口"）。

### `function_name(param1: str, param2: int = 0) -> bool`

- **所在模块**：`plugin_name.api`
- **参数说明**：
  - `param1`：说明
  - `param2`：说明，默认值 `0`
- **返回值**：说明返回内容及含义
- **调用示例**：

```python
from plugin_name.api import function_name

result = function_name("foo", 1)
```

## 附加功能

> 必填章节。列出指令之外的能力，例如定时任务、事件监听、Web 面板、数据存储、跨插件事件广播等；若无附加功能需明确写"无"。

- **定时任务**：说明触发周期与作用
- **事件监听**：说明监听的事件类型与处理逻辑
- **数据持久化**：说明存储位置与数据结构（如有）

## 依赖

- `astrbot`
- `<其他第三方依赖>`

## 常见问题

- **问题**：说明
  **解决**：说明

## 更新日志

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| `1.0.0` | `YYYY-MM-DD` | 初始版本 |

## 协议

`MIT` / `<其他协议>`
